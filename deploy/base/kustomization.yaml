# Kustomization for PodSweeper base resources
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: podsweeper-game

resources:
  - namespace.yaml
  - gamemaster-rbac.yaml
  - gamemaster-deployment.yaml
  - player-rbac.yaml
  - player-terminal.yaml
  - game-config.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: podsweeper
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false

# Images use :latest by default, which points to the most recent release.
# For local development, use the overlays/local overlay instead.
images:
  - name: ghcr.io/zwindler/podsweeper-gamemaster
    newTag: latest
  - name: ghcr.io/zwindler/podsweeper-hint-agent
    newTag: latest
  - name: ghcr.io/zwindler/podsweeper-player-terminal
    newTag: latest

# Patch to set hint-agent image env var (kustomize images only transforms container images)
patches:
  - target:
      kind: Deployment
      name: gamemaster
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: "ghcr.io/zwindler/podsweeper-hint-agent:latest"
